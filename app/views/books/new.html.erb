<h2>
	New book
</h2>
<%= form_with model: Book, url: books_url, method: :post, local: true do |form| %>
	  <%= form.text_field :name %>
		<%= form.text_field :author %>
		<%= form.submit "save"%>
<% end %>